class WlanConnector {
    connect = (path) => {
        // this.port?.on('data', (data) => {
        //     parseMessage(data.toString());
        // });
    };

    disconnect = () => {};

    send = (message) => {};

    isOpen = () => {};
}

module.exports = { WlanConnector };
